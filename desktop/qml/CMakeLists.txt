# QML files are shipped via the Qt Resource System embedded in the binary.
# qt_add_resources must be called in the same CMakeLists.txt as the target that
# will link the resource, so this file documents the file list only.
# The actual qt_add_resources call lives in ../ui/CMakeLists.txt.
