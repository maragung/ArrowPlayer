{
  "name": "eclipse-player-tooling",
  "version": "0.0.0",
  "private": true,
  "description": "Node-based repository gates only: the Markdown linter and the commit-message linter. No application code is built from here; see desktop/CMakeLists.txt.",
  "license": "MPL-2.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "lint:md": "markdownlint-cli2",
    "lint:commits": "commitlint --from origin/main --to HEAD --verbose",
    "lint:commit-msg": "commitlint --edit"
  },
  "devDependencies": {
    "@commitlint/cli": "21.2.2",
    "@commitlint/config-conventional": "21.2.2",
    "markdownlint-cli2": "0.23.2"
  }
}
